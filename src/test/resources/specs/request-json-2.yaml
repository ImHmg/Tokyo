name: request json 2
method: POST
endpoint: http://localhost:3000/json/j2/user
status: 200
queryParams:
  type: "json2type ${123}"
headers:
  Content-Type: application/json
rawBody: |
  {
    "name" : "${userName}",
    "id" : "${userId}"
  }

asserts:
  "one" : "@body json:$.user.id [==] 1"
  "three" : "@body raw"

captures:
  "userId" : "@body json:$.user.id"